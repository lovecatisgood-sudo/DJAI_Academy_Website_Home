// Ad serving is intentionally paused while djai.academy completes publisher
// approval recovery. Keeping this compatibility component avoids risky edits
// across article and directory templates while guaranteeing no ad inventory.
export default function AdSenseAd() {
  return null;
}
