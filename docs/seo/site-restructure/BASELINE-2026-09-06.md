# DJAI Site Restructure — Baseline 2026-09-06

## Repository state

- Production main before restructure: `763de3f`.
- Prepared SEO branch before rebase: `codex/djai-sitewide-seo-growth`, 16 commits ahead and 2 commits behind production.
- Implementation branch: `codex/djai-site-restructure`.
- Rebase result: 15 commits ahead of production; duplicate privacy-policy commit omitted because its released equivalent is already on main.
- Rebase conflicts retained the September 6 privacy dates, the valid promotion-header route guard, QR success journey, and September 2 SEO change dates.

## Live inventory observed before implementation

- Main sitemap: 345 URLs.
- Main route families: 267 tools, 36 blog, 14 Siamese Cat, 6 course, 5 Cam PDF, 3 portfolio, 3 Development, 3 Service, 3 privacy, 2 web promotion, and three home locale routes.
- School sitemap: 10 public locale URLs observed at `/th` and `/en` variants.
- Prepared ownership manifest: 110 rows, including 104 indexable and 6 gated non-indexable rows.

## Fresh local verification

### Full build

Command: `npm run build`  
Result: exit 0.

Built the main homepage, web promotion, paid course, QR, image, PDF, media, document, and Siamese Cat applications. The AdSense recovery script verified 449 static index pages.

### Test suite

Command: `npm test` after generated outputs existed  
Result: exit 0; every listed suite passed.

The initial pre-build run failed only because two integration assertions correctly required absent generated course files. After the complete build, both passed.

### Production-style audit

Command: `npm run verify:hostinger`  
Result: exit 0.

Reported:

- 323 pages;
- 22 redirects;
- 345 sitemap URLs, all reachable from the homepage link graph;
- 268 slash redirects;
- 522 internal links/assets;
- admin API authentication and canonical-host checks passed.

## Known baseline concerns

- Several application installs report high-severity dependency advisories. Their exploitability and runtime relevance have not yet been assessed; no automatic forced upgrade is authorized.
- Node emits module-type warnings for some `.js` ESM tests. These are non-failing baseline warnings and should be removed only with a tested package-boundary change.
- The prepared course contract still asserts that public discovery remains on `www`; that is the first intentional architecture change.
- The School locale behavior must be reconciled against its deployable source before any migration.
- The requested independent GPT-5.6 Luna max reviewer cannot be started because this session does not expose a sub-agent creation tool.
